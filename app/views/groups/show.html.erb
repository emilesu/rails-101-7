<div class="col-md-12">
  <div class="group">
    <%= link_to("编辑", edit_group_path(@group), class: "btn btn-primary pull-right") %>
  </div>

  <h2><%= @group.title%></h2>
  <div>开版时间：<%= @group.created_at %></div>
  <p><%= @group.description %></p>
</div>
